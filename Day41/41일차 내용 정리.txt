1. truncate -> 모든 행을 삭제 처리 한다(저장 공간까지 삭제) (rollback이 안 먹힌다)

2. 커맨드 창에서 데이터 베이스 파일 실행 하기
 - 학사관리 테이블 생성(SQL 명령어 사용) -> 우선 cmd창 경로를 sql 내부로 진입 후 mysql 접속 한다
 - MySQL 접속하기
 - mysql -u haksa_admin -p haksa_database
 - table.sql 파일을 이용한 데이터 삽입
 - SQL 파일을 c:\sql 이라는 폴더로 복사한다.
 - mysql>\. c:\sql\table.sql

3. 문자열 가져오기
 - substring("컬럼 이름",start index, count);

4. 정렬
 - 끝 부분에 order by "정렬 기준 컬럼" "(생략시오름차순정리) asc(오름)/desc(내림)"

5. flush privileges : 변경사항을 적용 하기 위해 사용

6. 현재 사용자 및 데이터 베이스 확인 명령어
 - select user();
 - select database();

7. 주석
 - #
 - /* */
 - --

8. 테이블 컬럼 수정
 - alter 구문을 사용하는데 modify가 들어감

9. view 생성
 - CREATE VIEW 뷰이름[(속성이름[,속성이름])]AS SELECT문;

10. view 목록 조회
 - SHOW FULL TABLES IN 디비명 WHERE TABLE_TYPE LIKE 'VIEW';

11. NATURE JOIN
 - 테이블간에 동일한 형식을 갖는 공통 컬럼이 반드시 하나만 존재해야 한다.
 * 이럴 경우 USING 절을 이용한 조인문을 이용하면 조인문을 구사할 수 있다.

 - 반드시 두 테이블 간의 동일한 이름, 타입을 가진 컬럼이 필요하다.
 - 조인에 이용되는 컬럼은 명시하지 않아도 자동으로 조인에 사용된다.
 - 동일한 이름을 갖는 컬럼이 있지만 데이터 타입이 다르면 에러가 발생한다.
 - 조인하는 테이블 간의 동일 컬럼이 SELECT 절에 기술되도 테이블 이름을 생략해야 한다.
 - 자연 조인이나 USING 절을 이용한 조인은 등가 조인만을 표현할 수 있다.

12. USING절 
 - USING 절은 조인에 사용될 컬럼을 지정한다.
 - NATURAL 절과 USING 절은 함께 사용할 수 없다.
 - 조인에 이용되지 않은 동일 이름을 가진 컬럼은 컬럼명 앞에 테이블명을 기술한다.
 - 조인 컬럼은 괄호로 묶어서 기술해야 한다.


*조인 관련 정보 * 
https://m.blog.naver.com/PostView.naver?isHttpsRedirect=true&blogId=islove8587&logNo=221049861998





