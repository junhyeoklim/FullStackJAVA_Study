/*(임시)유저 게시판 쿼리문*/

CREATE TABLE board (
    b_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    post_num INT NOT NULL,
    title VARCHAR(20) NOT NULL DEFAULT 'nothing',
    content VARCHAR(300) NOT NULL DEFAULT 'nothing',
    s_id INT,
    s_department CHAR(20) NOT NULL DEFAULT 'nothing',
    createTime DATETIME DEFAULT CURRENT_TIMESTAMP,
    updateTime TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    views BIGINT DEFAULT 0,
    commentCnt INT DEFAULT 0
);

* 게시물에 파일 업로드 관련 내용 
https://congsong.tistory.com/44
https://magicmk.tistory.com/54