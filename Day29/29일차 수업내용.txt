1. 반복자(Iterator)는 1회용이라 다시 사용할려면 다시 정의 해야 한다

2. 컬렉션 프레임 워크에서 HashSet : 중복 저장 x, 순서 저장이 아니다 ->집합의 성질
(하지만 사용자 정의 클래스에선 새로운 객체 생성이 되기 때문에 입력 값이 같아도 다른것으로 본다
따라서 사용자 정의 클래스를 만들떄는 hashCode(),equlas를 오버라이딩 해서 값을 비교 해야 한다)

3. return Objects.hash(name,age); //적절한 해쉬 코드를 만들어서 반환 시키는 용도니 가급적 이걸 사용


4. 컬렉션 프레임 워크에서 TreeSet : HashSet과 비슷하나 내부적으로 정렬 시켜준다